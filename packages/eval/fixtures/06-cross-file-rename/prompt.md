The function `formatLabel` is defined in `src/format.mjs` and used across the project.

Rename `formatLabel` to `wrapLabel` everywhere: its definition, every import, every call site, and every re-export. After your change there must be no remaining reference to the old name `formatLabel` anywhere in the source. Do not change its behavior.
