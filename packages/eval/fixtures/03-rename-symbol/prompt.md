In `src/area.mjs`, the exported function is named `calc`. Rename it to `rectangleArea` everywhere in the file, including the export and the internal call inside `describeArea`. Do not change any behavior.

After your edit, `src/area.mjs` must export a function named `rectangleArea` and must NOT export anything named `calc`.
