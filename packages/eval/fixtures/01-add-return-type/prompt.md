The function `add` in `src/math.ts` is missing an explicit return type annotation.

Edit `src/math.ts` so that the `add` function declares an explicit `: number` return type. Do not change its behavior or its parameters. Make the smallest edit necessary.
